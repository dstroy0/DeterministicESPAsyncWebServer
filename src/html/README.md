# Library HTML Assets

This directory contains the raw library HTML pages and components served to users by the web server.

These files serve as the source templates before they are processed (beautified, decorated, minified) and compressed (gzipped) into C++ binary array buffers (`uint8_t[]`) using the utilities in `src/utilities/`.
